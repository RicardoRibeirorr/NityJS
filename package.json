{
  "name": "nity-engine",
  "version": "1.0.0",
  "description": "A JavaScript game engine.",
  "main": "dist/nity.min.js",
  "scripts": {
    "build": "esbuild src/index.js --bundle --minify --format=iife --global-name=Nity --outfile=dist/nity.min.js && esbuild src/index.js --bundle --minify --format=esm --outfile=dist/nity.module.min.js",
    "start": "node dist/nity.min.js"
  },
  "keywords": ["game", "engine", "javascript"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.20.0"
  }
}
